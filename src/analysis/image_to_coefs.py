# this file goes through all the images, calculates coefficients for each
# and then stores the coefficients as a (n_subject, K) csv file in
# artifacts/, where K is the number of basis functions used (tentatively 512).